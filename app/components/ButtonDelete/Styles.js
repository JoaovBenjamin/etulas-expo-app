import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({

    button: {
        backgroundColor: 'red',
        padding: 12,
        borderRadius: 8,
    },

    texto:{
        display: 'flex',
        alignItems: 'center',
        gap: 5,
        textTransform:'uppercase',
        color:'white'
    }
})